#include <string>
#include <vector>

namespace tftp {
namespace terminal {
const int width = 68;   // character width
const int height = 28;  // line height
}  // namespace terminal
}  // namespace tftp
